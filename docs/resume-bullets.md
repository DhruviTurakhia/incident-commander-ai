# Resume wording

Use only after the project is running and published:

- Built a Python/FastAPI SRE incident commander that compiles reusable JSON workflows and
  correlates deployment, metric, log, trace, Kubernetes, commit, ticket, and runbook evidence into
  a root-cause timeline.
- Designed an asynchronous DAG executor with seven workflow primitives, retry state, SQLite
  persistence, versioned plans, conditional gates, and approval-controlled remediation.
- Created five reproducible incident scenarios and automated tests covering unsafe-plan rejection,
  evidence-linked investigations, pause/resume behavior, API flows, and audited rollback actions.

Short project line:

> **Incident Commander** — Python, FastAPI, MCP, Temporal, Slack, SQLite, Docker  
> Deterministic AI SRE workflow engine with evidence graphs and human-approved remediation.

Do not say:

- that this ran in a real production environment;
- that real customer infrastructure was connected;
- that the optional Slack, MCP, or Temporal integrations were deployed unless they are actually
  configured and demonstrated.

